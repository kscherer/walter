- name: "Parallel Serial"
  parallel:
    - name: "Serial"
      serial:
        - name: Serial 1
          command: echo "Parallel Serial 1" > test/temp/TaskParallelSerial1.tmp && stress --cpu 1 --timeout 1s
        - name: Serial 2
          command: echo "Parallel Serial 2" > test/temp/TaskParallelSerial2.tmp && stress --cpu 1 --timeout 3s
    - name: "Parallel 2"
      command: echo "Parallel 2" > test/temp/TaskParallelSerial3.tmp && stress --cpu 1 --timeout 3s

