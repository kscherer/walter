stages:
  - name: "build"
    tasks:
      - name: "setup build"
        command: "echo setting up"
      - name: "run build"
        command: "FOO=bar; echo building FOO=$FOO BUILD_ID=$BUILD_ID"
    cleanup:
      - name: "cleanup build"
        command: "echo cleanup build ..."

  - name: "deploy"
    tasks:
      - name: "run deploy"
        command: "echo deploying ..."
